const hostUrl = 'http://localhost:5000';

const productCategoryUrls = {
  defaultUrl: hostUrl + '/api/productCategory/getall',
  getAllUrl: hostUrl + '/api/productCategory/getAll'
};

export {
  productCategoryUrls
}